{
    "db_url": "${{ secrets.DB_URL }}",
    "jwttoken": {
        "key": "${{ secrets.JWT_TOKEN_KEY }}",
        "options": {
            "algorithm": "HS256",
            "expiresIn": "2 days",
            "issuer": "issuer"
        }
    }
}