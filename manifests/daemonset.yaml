apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    app: cocreatews
  name: cocreatews
  namespace: default
spec:
  selector:
    matchLabels:
      app: cocreatews
  template:
    metadata:
      labels:
        app: cocreatews
    spec:
      containers:
        - image: cocreatews
          imagePullPolicy: Always
          name: cocreatews
          ports:
            - containerPort: 3000
              name: app
              protocol: TCP
          env:
            - name: JWT_KEY
              valueFrom:
                secretKeyRef:
                  name: jwttoken
                  key: jwttoken
            - name: MONGO_URL
              valueFrom:
                secretKeyRef:
                  name: dburl
                  key: db_url
      imagePullSecrets:
        - name: github-package
      restartPolicy: Always
      nodeSelector:
        nodeType: application
